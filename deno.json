{
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true,
    "types": ["npm:vite/client", "./src/jsx.d.ts"]
  },
  "tasks": {
    "dev": "deno run -A npm:vite demo",
    "build": "deno run -A npm:vite build",
    "build-demo": "deno run -A npm:vite build demo",
    "test": "deno test -A"
  }
}