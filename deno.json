{
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true,
    "types": ["./src/jsx.d.ts"]
  },
  "tasks": {
    "dev": "deno run -A npm:vite demo",
    "build": "deno run -A npm:vite build demo",
    "test": "deno test -A",
    "build-lib": "deno run -A npm:vite build"
  }
}