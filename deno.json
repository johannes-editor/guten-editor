{
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true,
    "types": ["./src/jsx.d.ts"]
  },
  "tasks": {
    "dev": "deno run -A npm:vite demo",
    "build": "deno cache npm:vite && deno run -A npm:vite build --config vite.config.demo.ts",
    "test": "deno test -A"
  }
}